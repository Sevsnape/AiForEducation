# AIFOREC 数据库表结构

> 状态：与后端 Phase 1 同步  
> SQL 源文件：[sql/001_init.sql](./sql/001_init.sql)（含 `COMMENT ON` 表/列注释）  
> **白话逐表解析**：[数据库表结构解析.md](./数据库表结构解析.md)  
> 方言：PostgreSQL（本地可用 SQLite 适配，类型略放宽；`COMMENT ON` 以 PG 为准）

关联设计见 [系统设计草案.md](./系统设计草案.md)、[LangGraph框架思路.md](./LangGraph框架思路.md)。

---

## 1. ER 关系（逻辑）

```text
organizations ──< class_groups ──< memberships >── users
users ──< role_bindings
users ──< consents
users ──< threads ──< messages
              └────< session_summaries
users ── student_profiles
users ──< profile_corrections
users ──< artifacts
users ──< learning_events
users ──< question_asks
users ──< learning_plans
users ──< uploaded_files
users ──< weekly_summaries
* ──< audit_logs
```

---

## 2. 表说明

### 2.1 身份与租户

| 表 | 用途 |
|----|------|
| `users` | 账号主体（含登录邮箱、密码哈希） |
| `auth_sessions` | 登录会话（token 哈希、角色、过期/吊销） |
| `organizations` | 学校/机构（可空，个人版无组织） |
| `class_groups` | 班级/教学班 |
| `role_bindings` | 全局或组织内角色：student/teacher/admin |
| `memberships` | 班级成员关系（师生绑定，老师看学情的关系条件） |
| `consents` | 同意开关（可带 org 作用域）；**MVP 由管理员配置** |

**Consent 字段**

| 列 | 含义 |
|----|------|
| `learning_personalize` | 是否用画像个性化 |
| `history_retain` | 是否留存会话 |
| `share_learning_with_teacher` | 是否共享学情给老师 |

### 2.2 会话与历史

| 表 | 用途 | 可见性 |
|----|------|--------|
| `threads` | 会话线程 | 仅 owner |
| `messages` | 原文消息（含 counsel） | 仅 owner；老师 API 不查 |
| `session_summaries` | 会话摘要 | 学生；support_tags 不对老师 |

### 2.3 学生画像

| 表 | 用途 |
|----|------|
| `student_profiles` | 长期画像：`learning_json` + `support_json` |
| `profile_corrections` | 学生纠正记录 |

**硬规则**：任何老师查询路径不得 SELECT `support_json` / counsel 类 `messages`。

`learning_json` / `support_json` 字段形态见系统设计草案第 5 章。

### 2.4 学业产物

| 表 | 用途 |
|----|------|
| `artifacts` | 题包/练习包等结构化产物 |
| `learning_events` | 答题、完成练习等事件 |
| `question_asks` | 学业提问→模块标签（学情热点；不含 counsel） |
| `learning_plans` | 学生与 AI 共创学习计划（仅学习侧） |
| `uploaded_files` | 对话/出题上传材料元数据（见 005） |
| `weekly_summaries` | 成长周报（support_text 仅学生） |

详见 [学情分析与问题模块.md](./学情分析与问题模块.md)、[学习计划共创.md](./学习计划共创.md)、[文件对话与文件出题.md](./文件对话与文件出题.md)、[sql/003_question_asks.sql](./sql/003_question_asks.sql)、[sql/004_learning_plans.sql](./sql/004_learning_plans.sql)、[sql/005_uploads.sql](./sql/005_uploads.sql)。

### 2.5 审计

| 表 | 用途 |
|----|------|
| `audit_logs` | 鉴权、危机短路、导出、管理员访问等 |

---

## 3. 与 LangGraph 持久化的分工

| 存储 | 内容 |
|------|------|
| **业务 SQL（本文件）** | 用户、同意、消息原文、Profile、Artifact、question_asks、审计 |
| **LangGraph Checkpointer** | 同一 `thread_id` 的图执行中间态（续聊 / interrupt） |
| 不把 `support_json` 写入老师可读视图或检查点明文日志 |

Checkpointer 推荐生产用 Postgres（`langgraph-checkpoint-postgres`）；开发可用 Memory / SQLite。

---

## 4. 迁移约定

1. 以 `docs/sql/*.sql` 为源，按序号执行  
2. 改表同步更新本文与 SQLAlchemy 模型（`src/aiforec/infra/db/models.py`）  
3. 破坏性变更增加新文件（如 `002_xxx.sql`），勿改写已发布脚本语义  

---

## 5. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-10 | 首版：001_init.sql |
| 2026-08-10 | SQL 增加 COMMENT ON；新增《数据库表结构解析》 |
| 2026-08-10 | users 增 password_hash/last_login_at；新增 auth_sessions；002_auth.sql |
| 2026-08-10 | 新增 question_asks（003_question_asks.sql）；学情模块热点 |
| 2026-08-10 | 新增 learning_plans（004）；同意策略改管理员配置 |
| 2026-08-12 | 新增 uploaded_files（005）；文件对话与文件出题 |
